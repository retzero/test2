name: test kustomize

on:
  workflow_dispatch

jobs:
  inside-host:

    runs-on: ubuntu-latest

    steps:
      - uses: imranismail/setup-kustomize@v2
        with:
          kustomize-version: 10.15.1
