
name: Test

on:
  push:
  workflow_dispatch:

env:
  ACTIONS_STEP_DEBUG: true

jobs:
  build:
    runs-on: "ubuntu-latest"
    container: ubuntu:20.04
    steps:
      - run: apt-get update
      - uses: actions/checkout@v3
